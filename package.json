{
  "name": "pcto-angular",
  "version": "1.0.0",
  "description": "Angular frontend served by a TypeScript Express backend, replacing the previous Python Flask implementation.",
  "private": true,
  "scripts": {
    "build": "tsc",
    "start": "node dist-server/server.js",
    "serve": "npm start",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "express": "^4.18.4"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^20.14.2",
    "typescript": "^5.5.4"
  }
}
